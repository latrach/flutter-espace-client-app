import 'package:flutter/material.dart';

class MesContratsPage extends StatelessWidget {
  const MesContratsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Liste de mes contrats"));
  }
}
